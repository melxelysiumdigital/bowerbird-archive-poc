import { NextResponse } from 'next/server';

export async function GET() {
  // TODO: Implement Shopify OAuth flow
  return NextResponse.json({ message: 'Shopify OAuth endpoint — not yet implemented' });
}
