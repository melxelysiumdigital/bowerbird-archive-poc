query CartTransformRunInput {
  cart {
    lines {
      id
      quantity
      donationAmount: attribute(key: "_donation_amount") {
        value
      }
      donationDisplay: attribute(key: "Donation Amount") {
        value
      }
    }
  }
}
