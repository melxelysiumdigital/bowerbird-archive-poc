# Azure AI Search
AZURE_SEARCH_ENDPOINT=https://azure-ai-searchpoc.search.windows.net
AZURE_SEARCH_INDEX=fulltext-poc
AZURE_SEARCH_API_KEY=your-azure-search-api-key

# Shopify Storefront (public — exposed to browser)
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
NEXT_PUBLIC_SHOPIFY_STOREFRONT_TOKEN=your-storefront-token

# Shopify Admin (server-only — never exposed to browser)
# From the shopify-digitisation app (apps/shopify-digitisation/)
# Get the token: cd apps/shopify-digitisation && SHOPIFY_CLIENT_SECRET=<secret> pnpm get-admin-token
# Required scopes: read_customers, write_customers, read_draft_orders, write_draft_orders, read_orders
# Must have protected customer data access enabled (name + email)
SHOPIFY_ADMIN_ACCESS_TOKEN=your-admin-token

# Shopify Customer Account API (public — exposed to browser)
NEXT_PUBLIC_SHOPIFY_CUSTOMER_ACCOUNT_CLIENT_ID=your-client-id
NEXT_PUBLIC_SHOPIFY_SHOP_ID=your-numeric-shop-id

# Donation product handle (variants fetched automatically via Storefront API)
NEXT_PUBLIC_DONATION_PRODUCT_HANDLE=donation
